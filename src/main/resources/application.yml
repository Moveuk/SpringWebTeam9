spring:
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:springcoredb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  redis:
    host: localhost # 디폴트
    port: 6379 # 디폴트

  jpa:
    hibernate:
      format_sql: true
      show_sql: true
      ddl-auto: update

server:
  error:
    include-exception: false # Response에 Exception 표시 여부
    include-message: always # Response에 Exception Message 표시 여부
    include-stacktrace: on_param # Response에 Stack Trace 표시 여부
#  port: 9000


# aws s3
cloud:
  aws:
    credentials:
      accessKey: AKIAS6EAANAZHMDGSOES
      secretKey: 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
    s3:
      bucket: spartabucket09
    region:
      static: ap-northeast-2
    stack:
      auto: false